<script lang="ts">
    // En la PWA web no existe Liquid Glass nativo (es un módulo iOS). Se usa la
    // barra clásica, fiel a la variante Android/iOS sin liquid glass de pos_app.
    import ClassicTabBar from './tabbar/ClassicTabBar.svelte'
</script>

<ClassicTabBar />
