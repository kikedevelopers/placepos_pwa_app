export type Category = {
    id: number
    name: string
    is_archived: boolean
    created_at: string
}

export type Packaging = {
    id: number
    name: string
    value: number
    is_archived: boolean
    created_at: string
}
