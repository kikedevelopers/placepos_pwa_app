import type { Component } from 'svelte'
import type { IconProps } from '@lucide/svelte'

/** Tipo de un icono de lucide-svelte (equivalente a LucideIcon en pos_app). */
export type IconComponent = Component<IconProps>
